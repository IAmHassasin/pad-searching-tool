import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hassasin.padsearchingtool',
  appName: 'PAD Searching Tool',
  webDir: 'dist-android'
};

export default config;
