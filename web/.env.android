# Used only for the Android (Capacitor) build — see `npm run build:android`.
# The packaged app has no server of its own, so every API call must be an
# absolute URL pointing at the deployed backend.
VITE_API_BASE=https://app.example.com
