{
  "name": "pst-gateway",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker — path router for the public app host",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
