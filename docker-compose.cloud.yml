# Oracle / production: working DB on named volume only (no host seed bind-mount).
# Use: docker compose -f docker-compose.yml -f docker-compose.cloud.yml up -d --build
services:
  app:
    volumes:
      - dadguide_working_sqlite:/data
